# Copy to .env and fill in. .env is gitignored and must never be committed.

# Base URL of the system under test. Always a dev host - never point at production.
BASE_URL=https://dev.app.agnoshealth.com

# Backend API, used by tests that assert on the API contract directly.
API_URL=https://dev.api.agnoshealth.com

# Shared dashboard account from the assignment brief.
TEST_EMAIL=test@gmail.com
TEST_PASSWORD=12345
